/* rlottie */
#define RLOTTIE_VERSION "${RLOTTIE_VERSION}"
${define LOTTIE_MODULE}

#ifdef LOTTIE_MODULE
#define LOTTIE_IMAGE_MODULE_SUPPORT

#define LOTTIE_IMAGE_MODULE_PLUGIN ${default LOTTIE_MODULE_PATH ""}
#endif

${define LOTTIE_THREAD}

#ifdef LOTTIE_THREAD
#define LOTTIE_THREAD_SUPPORT
#endif

${define LOTTIE_CACHE}

#ifdef LOTTIE_CACHE
#define LOTTIE_CACHE_SUPPORT
#endif

/* lottie2img */
#define LOTTIE2IMG_VERSION "${VERSION}"
#define LOTTIE2IMG_MULT_THREAD ${default LOTTIE2IMG_MULT_THREAD false}
#if LOTTIE2IMG_MULT_THREAD
#define LOTTIE2IMG_ASYNC
#endif